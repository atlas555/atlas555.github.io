---
title: '小学·汉字学习'
subtitle: '小学拼音、汉字'
author: 张晓龙
date: 2025-03-14
slug: 'hanzi'
show_toc: false
# customCSS: ["zdict/learn-chars.css"]
# customJS: ["hanzi-writer.min.js"]
---
<script src="https://cdn.jsdelivr.net/npm/hanzi-writer@3.5/dist/hanzi-writer.min.js"></script>

<script>
function renderFanningStrokes(target, strokes) {
  var svg = document.createElementNS('http://www.w3.org/2000/svg', 'svg');
  svg.style.width = '75px';
  svg.style.height = '75px';
  svg.style.border = '1px solid #EEE'
  svg.style.marginRight = '3px'
  target.appendChild(svg);
  var group = document.createElementNS('http://www.w3.org/2000/svg', 'g');

  // set the transform property on the g element so the character renders at 75x75
  var transformData = HanziWriter.getScalingTransform(75, 75);
  group.setAttributeNS(null, 'transform', transformData.transform);
  svg.appendChild(group);

  strokes.forEach(function(strokePath) {
    var path = document.createElementNS('http://www.w3.org/2000/svg', 'path');
    path.setAttributeNS(null, 'd', strokePath);
    // style the character paths
    path.style.fill = '#555';
    group.appendChild(path);
  });
}

HanziWriter.loadCharacterData('彩').then(function(charData) {
  var target = document.getElementById('grid-background-target');
  for (var i = 0; i < charData.strokes.length; i++) {
    var strokesPortion = charData.strokes.slice(0, i + 1);
    renderFanningStrokes(target, strokesPortion);
  }
});
// document.addEventListener('DOMContentLoaded', function() {
//   var writer = HanziWriter.create('character-target-div', '国', {
//     width: 200,
//     height: 200,
//     padding: 5,
//     strokeAnimationSpeed: 2,
//     delayBetweenStrokes: 10, // milliseconds
//     radicalColor: '#337ab7' // blue
//     showOutline: true,
//     strokeColor: '#333',
//     outlineColor: '#DDD',
//     highlightColor: '#FF0000'
//   });
  
  document.getElementById('animate-button').addEventListener('click', function() {
    writer.animateCharacter();
  });
</script>

<svg xmlns="http://www.w3.org/2000/svg" width="100" height="100" id="grid-background-target">
  <line x1="0" y1="0" x2="100" y2="100" stroke="#DDD" />
  <line x1="100" y1="0" x2="0" y2="100" stroke="#DDD" />
  <line x1="50" y1="0" x2="50" y2="100" stroke="#DDD" />
  <line x1="0" y1="50" x2="100" y2="50" stroke="#DDD" />
</svg>

<div>
  <button id="animate-button" class="animate-button">演示书写</button>
</div>
